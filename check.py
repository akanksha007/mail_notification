import parser, time,pynotify
#import modules
x=1
#set variable
while True:
    parser.mail(0)
    time.sleep(600)
#re-run program to infinity, every ten seconds. Change the "time.sleep(seconds to wait) variable if you want a longer/shorter check interval. 
